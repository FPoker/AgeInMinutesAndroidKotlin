# AgeInMinutesAndroidKotlin

A basic app made in Android Studio using Kotlin language that converts an age (the user selects a date) into minutes passed since the selected date until the current date.
